---
"knox-wallet": patch
---

Configure MPP requests with an auto-managed Tempo deposit derived from the parsed payment challenge, including session `suggestedDeposit` values, with `--mpp-deposit` available as a request-level override.
