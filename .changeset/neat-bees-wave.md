---
"knox-wallet": minor
---

Remove the `--no-plugins` global flag so plugin hooks always run during paid requests and `knox account status`.

For plugin troubleshooting, use `knox plugins list` and `knox plugins setup <plugin-name>`.
