---
"knox-wallet": minor
---

Remove the `--plugins-timeout-ms` global flag and use a fixed internal plugin timeout.

Also update CLI docs and skill documentation to remove references to the removed flag.
